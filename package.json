{
  "name": "oml-omnimantic-lattice",
  "version": "1.0.0",
  "description": "OML (Omni-Mantic Lattice) — a unified computational typology synthesizing world divination systems and modern personality frameworks",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.js",
    "serve": "node scripts/serve.js",
    "test:browser": "node tests/browser/run.js",
    "verify": "npm test && npm run test:browser"
  },
  "devDependencies": {
    "playwright": "1.56.1"
  }
}
